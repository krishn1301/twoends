<?xml version="1.0" encoding="utf-8"?>
<!--
  The same widget in a row: the pair on the left, the figure on the right.

  For the home screen that is already full. It shows the bare number rather than
  the number with its unit, because at this height there is one line and a unit
  costs three characters that the eyebrow above it already implies.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="140dp"
    android:minHeight="70dp"
    android:targetCellWidth="2"
    android:targetCellHeight="1"
    android:minResizeWidth="110dp"
    android:minResizeHeight="70dp"
    android:description="@string/widget_distance_strip_description"
    android:updatePeriodMillis="0"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:previewImage="@drawable/widget_preview_distancestrip"
    android:initialLayout="@layout/glance_default_loading_layout" />
